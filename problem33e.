#include <stdio.h>

int main() 
{
    int sum = 0; 
    int term;   
    
    for (int i = 0; i < 50; i++)
    {
        term = 1 + 3 * i; 
        sum += term;      
    }
    printf("The total of the first 50 numbers in the series 1, 4, 7, 10, ... is: %d\n", sum);

    return 0;
}
